# RFM Analysis App - Environment Variables
# Zkopíruj tento soubor jako .env.local a vyplň hodnoty

# Next.js
NEXT_PUBLIC_APP_NAME="RFM Analýza"
NEXT_PUBLIC_APP_VERSION="1.0.0"

# Google Analytics (volitelné)
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"

# API Keys (pokud budete potřebovat v budoucnu)
# NEXT_PUBLIC_API_URL="https://api.example.com"
# API_SECRET_KEY="your-secret-key"

# Vercel Analytics (automaticky aktivní na Vercelu)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID="automatically-set-by-vercel"

# ============================================
# Google OAuth 2.0 - Export do Google Sheets
# ============================================

# Google Cloud Console -> APIs & Services -> Credentials
# Vytvořte OAuth 2.0 Client ID (Web application type)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_client_secret_here

# Redirect URI musí být přidána do Google Cloud Console jako Authorized redirect URI
# Pro development:
NEXT_PUBLIC_REDIRECT_URI=http://localhost:3000/api/auth/callback/google

# Pro production (Vercel):
# NEXT_PUBLIC_REDIRECT_URI=https://your-domain.vercel.app/api/auth/callback/google

# Potřebné Google APIs (zapnout v Google Cloud Console):
# - Google Sheets API
# - Google Drive API

# OAuth Scopes (automaticky nastaveny v aplikaci):
# - https://www.googleapis.com/auth/drive.file
#
# DŮLEŽITÉ: Používáme pouze 'drive.file' scope místo 'spreadsheets'
# Výhody:
# ✓ NEVYŽADUJE Google App Verification (jinak 6+ týdnů proces)
# ✓ Přístup POUZE k souborům vytvořeným aplikací (vyšší bezpečnost)
# ✓ Plně postačuje pro vytváření a editaci Google Sheets
# ⚠️ Zobrazí "unverified app" varování, ale lze přeskočit (Advanced → Continue)
#
# Google Cloud Console nastavení:
# 1. APIs & Services → OAuth consent screen
# 2. Scopes → Edit
# 3. Přidat pouze: ../auth/drive.file
# 4. NEPOUŽÍVAT: ../auth/spreadsheets (vyžaduje verifikaci)